export enum EVENTS_NAME {
  chestLoot = 'chest-loot',
  attack = 'attack',
  gameEnd = 'game-end',
}

export enum GameStatus {
  WIN,
  LOSE,
}

export enum chestID {
  normalChest = 595,
}

export enum enemyID {
  normalEnemy = 503,
}
