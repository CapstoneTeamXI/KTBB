export enum EVENTS_NAME {
  chestLoot = "chest-loot",
  attack = "attack",
  gameEnd = "game-end",
  enemyKilled = "enemy-killed",
}

export enum GameStatus {
  WIN,
  LOSE,
}

export enum chestID {
  normalChest = 595,
}

export enum enemyID {
  level1Orc = 87,
  level2Orc = 439,
  level3Orc = 375,
  level4Orc = 503,
}
